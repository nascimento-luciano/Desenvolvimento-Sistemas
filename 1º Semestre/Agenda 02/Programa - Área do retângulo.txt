//Programa para calcular a área do retângulo

 #incluir biblioteca          //biblioteca de função de entrada e saída
 
 função de início()           //retorna um número inteiro
 número flutuante base, altura, area;
 escreva um valor ("altura = "); o valor digitado será o conteúdo da variável a;
 escreva um valor ("base = "); o valor digitado será o conteúdo da variável b;
 
 c = a*b;                     //exibe o valor da operação
 
 escreva ("Área do retângulo é = %f m^2 \n", area) //exibe o valor da área do retângulo na tela
 
 sistema (pausa)              //pausa a tela
 
 retorne 0;                  //finaliza a execução da função